if(NOT TARGET tst_desousa_pico)
    add_library(tst_desousa_pico INTERFACE)
    target_include_directories(tst_desousa_pico INTERFACE ${CMAKE_CURRENT_LIST_DIR})
    target_sources(tst_desousa_pico INTERFACE ${CMAKE_CURRENT_LIST_DIR}/tst.c)
endif()